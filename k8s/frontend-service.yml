apiVersion: v1
kind: Service
metadata:
  name: ai-ops-frontend
  namespace: ai-ops
  labels:
    app: ai-ops-frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: ai-ops-frontend
