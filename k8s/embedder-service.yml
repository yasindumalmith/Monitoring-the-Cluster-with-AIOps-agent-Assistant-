apiVersion: v1
kind: Service
metadata:
  name: ai-ops-embedder
  namespace: ai-ops
  labels:
    app: ai-ops-embedder
spec:
  type: ClusterIP
  selector:
    app: ai-ops-embedder
  ports:
    - port: 8001
      targetPort: 8001
      name: http
